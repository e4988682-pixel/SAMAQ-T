import 'package:flutter/material.dart';

void main() {
  runApp(SamaqApp());
}

class SamaqApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false, 
      home: Scaffold(
        backgroundColor: Colors.black, // الشاشة السوداء
        body: Center( 
          child: Text(
            "أهلاً بك في سمقَ !",
            style: TextStyle(
              color: Colors.blue, // الخط الأزرق
              fontSize: 32, 
              fontWeight: FontWeight.bold, 
            ),
          ),
        ),
      ),
    );
  }
}
